<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportygo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportyGo">
        <activity android:name=".login.SignupActivity"></activity>
        <activity android:name=".cart.CartActivity" />
        <activity
            android:name=".profile.EditProfileActivity"
            android:label="@string/title_activity_edit_profile"
            android:theme="@style/Theme.SportyGo.NoActionBar" />
        <activity android:name=".review.ReviewActivity" />
        <activity android:name=".shipping.ShippingActivity" />
        <activity android:name=".review.ReviewFormActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".product.ProductActivity" />
        <activity android:name=".payment.PaymentActivity" />
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".contact.ContactActivity" />
        <activity android:name=".checkout.CheckoutActivity" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>